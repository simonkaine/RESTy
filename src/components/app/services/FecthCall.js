import React from 'react';

// each time checked and submit happens in Form/ checks to see which method to use based off radio buttons
// the url; method is grabbing the header + body info
// returning the correct radio button method 

export const fetchhCall = async ({ url, method, body }) => {
    const url = await fetch() 
}